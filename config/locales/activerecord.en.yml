en:
  activerecord:
    models:
      test: Test

    attributes:
      test:
        title: Title
        level: Level
        category: Category

    errors:
      models:
        test:
          attributes:
            category:
              required: is required
              title:
                blank: ca't be blank
